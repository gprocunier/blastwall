apiVersion: batch/v1
kind: Job
metadata:
  name: blastwall-spo-validation
  namespace: blastwall-workloads
  labels:
    app.kubernetes.io/name: blastwall
    app.kubernetes.io/component: validation
    blastwall.io/profile: openshift-spo
    blastwall.io/workload-class: blastwall
  annotations:
    openshift.io/required-scc: blastwall-confined
    blastwall.io/source: openshift/spo/tests
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: blastwall-spo-validation
        blastwall.io/profile: openshift-spo
        blastwall.io/workload-class: blastwall
      annotations:
        openshift.io/required-scc: blastwall-confined
    spec:
      restartPolicy: Never
      serviceAccountName: blastwall-runner
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: probe
          image: registry.access.redhat.com/ubi9/python-312:latest
          command:
            - python3
            - /opt/blastwall/blastwall_spo_probe.py
          env:
            - name: BLASTWALL_EXPECTED_SELINUX_TYPE
              value: __BLASTWALL_SPO_STANDARD_SELINUX_TYPE__
            - name: BLASTWALL_PROFILE_CLASS
              value: standard
          volumeMounts:
            - name: probe
              mountPath: /opt/blastwall
              readOnly: true
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
      volumes:
        - name: probe
          configMap:
            name: blastwall-spo-probe
            defaultMode: 0555
---
apiVersion: batch/v1
kind: Job
metadata:
  name: blastwall-nested-spo-validation
  namespace: blastwall-workloads
  labels:
    app.kubernetes.io/name: blastwall
    app.kubernetes.io/component: validation
    blastwall.io/profile: openshift-spo
    blastwall.io/workload-class: blastwall-nested
  annotations:
    openshift.io/required-scc: blastwall-nested
    blastwall.io/source: openshift/spo/tests
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: blastwall-nested-spo-validation
        blastwall.io/profile: openshift-spo
        blastwall.io/workload-class: blastwall-nested
      annotations:
        openshift.io/required-scc: blastwall-nested
    spec:
      hostUsers: false
      restartPolicy: Never
      serviceAccountName: blastwall-nested-runner
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: probe
          image: registry.access.redhat.com/ubi9/python-312:latest
          command:
            - python3
            - /opt/blastwall/blastwall_spo_probe.py
          env:
            - name: BLASTWALL_EXPECTED_SELINUX_TYPE
              value: __BLASTWALL_SPO_NESTED_SELINUX_TYPE__
            - name: BLASTWALL_PROFILE_CLASS
              value: nested
          volumeMounts:
            - name: probe
              mountPath: /opt/blastwall
              readOnly: true
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
      volumes:
        - name: probe
          configMap:
            name: blastwall-spo-probe
            defaultMode: 0555
